apiVersion: v1
kind: ServiceAccount
metadata:
  name: simple-dump-server-test
  labels:
    app: simple-dump-server-test
    heritage: {{.Release.Service | quote}}
    release: {{.Release.Name | quote}}
    chart: {{ .Chart.Name }}
